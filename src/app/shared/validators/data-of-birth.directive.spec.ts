import { DataOfBirthDirective } from './data-of-birth.directive';

describe('DataOfBirthDirective', () => {
  it('should create an instance', () => {
    const directive = new DataOfBirthDirective();
    expect(directive).toBeTruthy();
  });
});
