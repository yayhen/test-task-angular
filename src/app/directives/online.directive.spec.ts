import { OnlineDirective } from './online.directive';

describe('OnlineDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlineDirective();
    expect(directive).toBeTruthy();
  });
});
